//An enum for the direction to move in for code clarity. Containds left, right, up, and down

public enum Direction //Direction Enum
{ //Direction Start
	
	UP, DOWN, LEFT, RIGHT //Creates the static options for the enum
	
} //Direction End